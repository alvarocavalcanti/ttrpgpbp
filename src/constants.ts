// Global, configurable knobs. Keep in sync with the SQL constant in
// join_channel (supabase/migrations/..._channel_cap_and_join_system_message.sql)
// when changing.
export const MAX_CHANNELS_PER_USER = 10
